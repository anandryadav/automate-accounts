# Python virtual environment
venv
.venv
__pycache__
*.pyc

# IDE / OS specific
.idea
.vscode
*.DS_Store
.env

# Ignore everything inside the uploads directory...
uploads/*

# ...except the uploads directory itself (so it stays in the repo)
!uploads/.gitkeep
